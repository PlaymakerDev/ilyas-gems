import React from 'react'

interface Props {

}

const Footer: React.FC<Props> = (props) => {
  const { } = props

  return (
    <footer>index</footer>
  )
}

export default React.memo<Props>(Footer)
